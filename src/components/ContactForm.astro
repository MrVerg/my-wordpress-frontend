---
---

<section id="contacto" class="py-24 bg-white overflow-hidden">
  <div class="container-custom">
    <div class="bg-brand-coral p-12 md:p-20 rounded-[4rem] shadow-2xl relative text-white">
      <!-- Decorative Circle -->
      <div class="absolute -bottom-12 -left-12 w-64 h-64 border-[20px] border-white/20 rounded-full"></div>
      
      <div class="max-w-3xl mx-auto relative z-10 text-center lg:text-left grid grid-cols-1 lg:grid-cols-[1fr,1.2fr] gap-16 items-center">
        <div>
          <span class="inline-block px-4 py-1 rounded-full bg-white/20 text-white font-black uppercase tracking-widest text-sm mb-6">
            Contacto
          </span>
          <h2 class="text-5xl md:text-6xl font-black uppercase leading-tight mb-8">
            ¿LISTO PARA EMPEZAR?
          </h2>
          <p class="text-xl font-bold text-white/90">
            Escríbenos y nuestro equipo se contactará contigo en menos de 24 horas. ¡Hagamos algo increíble!
          </p>
        </div>

        <form class="space-y-6 text-brand-dark">
          <div class="grid grid-cols-1 gap-4">
            <input 
              type="text" 
              placeholder="NOMBRE COMPLETO" 
              class="w-full bg-white border-4 border-brand-dark rounded-2xl px-6 py-4 font-black uppercase placeholder:text-brand-dark/30 outline-none focus:ring-8 focus:ring-brand-lime/50 transition-all"
            />
            <input 
              type="email" 
              placeholder="CORREO ELECTRÓNICO" 
              class="w-full bg-white border-4 border-brand-dark rounded-2xl px-6 py-4 font-black uppercase placeholder:text-brand-dark/30 outline-none focus:ring-8 focus:ring-brand-lime/50 transition-all"
            />
            <textarea 
              rows="4" 
              placeholder="TU MENSAJE..." 
              class="w-full bg-white border-4 border-brand-dark rounded-2xl px-6 py-4 font-black uppercase placeholder:text-brand-dark/30 outline-none focus:ring-8 focus:ring-brand-lime/50 transition-all resize-none"
            ></textarea>
          </div>
          
          <button type="submit" class="w-full btn-pill bg-brand-dark text-white hover:bg-brand-purple border-b-8 border-black shadow-none mt-4">
            ENVIAR AHORA ⚡
          </button>
        </form>
      </div>
    </div>
  </div>
</section>
